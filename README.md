# code-sandbox

> Work in progress — isolated code execution service written in Go.

Compiles and runs untrusted source code inside Linux namespace sandboxes
and returns stdout / stderr with per-test verdict.
